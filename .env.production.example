# Configuração de Produção para dei.bet
# Copie este arquivo para .env e ajuste as configurações conforme necessário

APP_NAME="Dei Bet"
APP_ENV=production
APP_KEY=base64:lqJP4xhImGxoySjSL647jlthPQkQLyfkDTwmNzkNB5s=
APP_DEBUG=false
APP_DEMO=false
APP_URL=https://dei.bet
VITE_BASE_URL=/
FILAMENT_BASE_URL=onlyadm

# EuPago Configuration
EUPAGO_ENV=prod
EUPAGO_API_KEY=068b-8b22-2bf7-86df-6397
EUPAGO_CHANNEL="XP Create"

# Logging - Configuração otimizada para produção
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Database - Configure com suas credenciais reais
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=deibet_production
DB_USERNAME=deibet_user
DB_PASSWORD=sua_senha_segura_aqui

# Cache e Session - Redis recomendado para produção
BROADCAST_DRIVER=pusher
CACHE_DRIVER=redis
FILESYSTEM_DISK=local
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120

# Redis Configuration
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Email Configuration - Sistema otimizado com fallback
MAIL_MAILER=failover
MAIL_HOST=smtp.postmarkapp.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=support@dei.bet
MAIL_FROM_NAME="Dei Bet"

# Postmark Configuration (recomendado para produção)
POSTMARK_TOKEN=seu_token_postmark_aqui

# AWS Configuration (se usar S3 para storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=eu-west-1
AWS_BUCKET=deibet-storage
AWS_USE_PATH_STYLE_ENDPOINT=false

# Pusher Configuration
PUSHER_APP_ID=6138611
PUSHER_APP_KEY=sua_chave_pusher
PUSHER_APP_SECRET=seu_secret_pusher
PUSHER_HOST=dei.bet
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=eu

# Vite Configuration
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# JWT Configuration
JWT_SECRET=Yi65LYZGMxNYKOm5VIeisqiDFoV9Z410sAwVn8ydhwksruujPfUTrMK9C7VhNVOM
JWT_TTL=6000000

# Google Services (opcional)
GOOGLE_RECAPTCHA_ENABLE=true
GOOGLE_RECAPTCHA_SITE_KEY=sua_chave_site_recaptcha
GOOGLE_RECAPTCHA_SECRET_KEY=sua_chave_secreta_recaptcha

GOOGLE_MAPS_ENABLE=false
GOOGLE_MAPS_KEY=

GOOGLE_ANALYTICS_ENABLE=true
GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Social Login (opcional)
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_REDIRECT="${APP_URL}/auth/callback/twitter"

LINKEDIN_API_KEY=
LINKEDIN_API_SECRET=
LINKEDIN_REDIRECT="${APP_URL}/auth/callback/linkedin"

FACEBOOK_API_KEY=
FACEBOOK_API_SECRET=
FACEBOOK_REDIRECT="${APP_URL}/auth/callback/facebook"

GOOGLE_API_KEY=
GOOGLE_API_SECRET=
GOOGLE_REDIRECT="${APP_URL}/auth/callback/google"

# Stripe Configuration (se usar)
STRIPE_ENABLED=on
STRIPE_SUBSCRIPTION_ENABLED=on
STRIPE_BASE_URI=https://api.stripe.com
STRIPE_WEBHOOK_URI="${APP_URL}/webhooks/stripe"
STRIPE_WEBHOOK_SECRET=whsec_sua_chave_webhook
STRIPE_KEY=pk_live_sua_chave_publica
STRIPE_SECRET=sk_live_sua_chave_secreta

# Configurações específicas de produção
TELESCOPE_ENABLED=false
DEBUGBAR_ENABLED=false
HORIZON_ENABLED=true

# Configurações de segurança
SANCTUM_STATEFUL_DOMAINS=dei.bet,www.dei.bet
SESSION_DOMAIN=.dei.bet
SESSION_SECURE_COOKIE=true
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax
